# ============================================================================
# PXM-API-Manager Environment Configuration
# ============================================================================
# Copy this file to .env.local and update with your values for development
# For production, use .env.production or set these via deployment platform
#
# IMPORTANT: Never commit .env files with sensitive data to version control
#

# ============================================================================
# DEBUG MODE
# ============================================================================
# Set to 'true' to enable debug mode (displays debug login button)
# This bypasses CyberArk authentication and allows testing without credentials
# ONLY USE IN DEVELOPMENT - DISABLE IN PRODUCTION
#
# Default: false
VITE_DEBUG_MODE=true

# ============================================================================
# CYBERARK API CONFIGURATION (Optional - can be set via login form)
# ============================================================================
# The application will prompt for these on login if not set here
# For automated deployments, you can pre-configure these values
#

# CyberArk PVWA Base URL
# Example: https://cyberark.company.com:8443
# This is prompted during login, so it's optional in .env
# VITE_CYBERARK_API_URL=https://your-cyberark-instance:8443

# CyberArk Authentication Token (recommended to NOT set this - use login form)
# If set, the app will use this token instead of prompting for credentials
# For security, this should only be used in secure CI/CD environments
# VITE_CYBERARK_TOKEN=your-bearer-token-here

# ============================================================================
# APPLICATION METADATA
# ============================================================================
# These are optional and can be set for analytics or monitoring
#

# Application Version
VITE_VERSION=1.0.0

# Environment Name (development, staging, production)
VITE_ENV=production

# ============================================================================
# DEPLOYMENT INSTRUCTIONS
# ============================================================================
#
# 1. LOCAL DEVELOPMENT:
#    - Copy this file to .env.local
#    - Set VITE_DEBUG_MODE=true if you want to skip login during dev
#    - Run: npm run dev
#
# 2. PRODUCTION BUILD:
#    - Create .env.production file with production settings
#    - Set VITE_DEBUG_MODE=false
#    - Build: npm run build
#    - Deploy dist/ folder to your web server
#
# 3. DOCKER DEPLOYMENT:
#    - Build: docker build -t pxm-api-manager .
#    - Run: docker run -p 80:80 -e VITE_DEBUG_MODE=false pxm-api-manager
#
# 4. ENVIRONMENT VARIABLES AT RUNTIME:
#    - Vite requires VITE_ prefix for client-side variables
#    - Build with: npm run build
#    - Variables must be set before build time (not runtime)
#
